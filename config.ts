
export const CONFIG = {
  /**
   * Set to true to use the Express/MySQL backend.
   * Set to false to use the local browser storage (Frontend only).
   */
  USE_BACKEND: false,
  
  /**
   * The base URL for the backend API.
   */
  API_URL: 'http://localhost:3001/api'
};
